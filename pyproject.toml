[project]
name = "partition-sequencing-within-intervals"
version = "0.0.0"
description = "AutoResearch campaign: Partition to Sequencing Within Intervals"
requires-python = ">=3.12,<3.14"
dependencies = ["ortools==9.15.6755"]

[tool.uv]
package = false
